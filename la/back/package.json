{
  "name": "turno-residencia",
  "version": "1.0.0",
  "main": "lascrap.js",
  "scripts": {
    "postinstall": "npx puppeteer install",
    "test": "echo \"Error: no test specified\" && exit 1",
    "check-puppeteer": "node -e \"const puppeteer = require('puppeteer'); puppeteer.launch().then(() => console.log('Puppeteer is working')).catch(err => console.error('Puppeteer failed', err));\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "node-notifier": "^10.0.1",
    "proxy-chain": "^2.4.0",
    "puppeteer": "^22.9.0",
    "xlsx": "^0.18.5"
    
  }
}
